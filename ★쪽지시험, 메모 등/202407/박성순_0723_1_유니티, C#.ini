Unity
    유니티 UI 다루기
    Navigation > OffMeshLink
        1. BloodScreen
        2. 탄창 UI 구현

    Unity에서 List<> ex)
        List<Transform> trList = new List<Transform>();
        List<GameObject> objList = new List<GameObject>();

C#
    인덱서 :
        객체 인덱스에 접근하는 프로퍼티
    List :
        List<T> 클래스 :
            배열과 유사한 컬렉션 클래스
            동적으로 크기가 조절되는 배열
            배열보다 편리한 기능 제공
            ArrayList와 같은 기능을 하며, 사용법도 비슷

            ArrayList와 List의 차이?
            형식 제약 where T : struct
            
